# 管理端「墙钟时间」使用的 IANA 时区（考试批次创建/筛选、与日期选择器一致）
# 未设置时使用浏览器本机时区
VITE_DISPLAY_TIMEZONE=Asia/Shanghai

# 前端请求 API 基址（可填完整域名，或 /api 走网关/反向代理）
VITE_API_BASE_URL=/api

# 仅开发模式（vite dev）使用的代理目标
VITE_DEV_API_PROXY_TARGET=http://127.0.0.1:8002
